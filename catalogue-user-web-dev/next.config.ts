import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.unsplash.com", "media.istockphoto.com", "encrypted-tbn0.gstatic.com", "imageio.forbes.com", "w7.pngwing.com"]
  }
};

export default nextConfig;
